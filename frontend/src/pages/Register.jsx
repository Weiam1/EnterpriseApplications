import { Box, Typography } from "@mui/material";

const Register = () => {
    return (
        <Box sx={{ padding: 6 }}>
            <Typography variant="h3" fontWeight="bold">
                Register Page
            </Typography>
        </Box>
    );
};

export default Register;
